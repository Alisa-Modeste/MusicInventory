<!-- All tracks in this album -->

<ul>
  <% @album.tracks.each do |track| %>
    <li><%= link_to track.name, band_album_track_url(params[:band_id],
      params[:album_id], track.id) %></li>
  <% end %>

</ul>